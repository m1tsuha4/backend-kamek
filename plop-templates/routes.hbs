const express = require('express');  
const router = express.Router();  
const {{pascalCase name}}Controller = require('../controllers/{{camelCase name}}Controller');  

router.post('/{{ulerCase name}}', {{pascalCase name}}Controller.create);  
router.get('/{{ulerCase name}}', {{pascalCase name}}Controller.getAll);  
router.get('/{{ulerCase name}}/:id', {{pascalCase name}}Controller.getById);  
router.put('/{{ulerCase name}}/:id', {{pascalCase name}}Controller.update);  
router.delete('/{{ulerCase name}}/:id', {{pascalCase name}}Controller.delete);  
  
module.exports = router;  
